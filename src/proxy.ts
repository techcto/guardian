import{NextRequest,NextResponse}from'next/server';import{sessionCookie,verifySession}from'@/lib/session';
export async function proxy(req:NextRequest){const path=req.nextUrl.pathname;if(path.startsWith('/login')||path.startsWith('/api/auth/')||path==='/api/health'||path.startsWith('/api/v1/agents/')||path==='/api/billing/webhook')return NextResponse.next();const session=await verifySession(req.cookies.get(sessionCookie)?.value,process.env.GUARDIAN_SESSION_SECRET??'');if(session)return NextResponse.next();const url=req.nextUrl.clone();url.pathname='/login';url.search='';return NextResponse.redirect(url)}
export const config={matcher:['/((?!_next/static|_next/image|favicon.ico|guardian-mark.svg|robots.txt).*)']};
