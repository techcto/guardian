const cards=[['Agent status','Offline-safe'],['Active incidents','0'],['Protected origins','0'],['Queue depth','0']];
export default function Home(){return <main><section className="hero"><div className="eyebrow">Detect · Alert · Respond · Recover</div><h1>Your AI-powered infrastructure guardian.</h1><p className="muted">Guardian.US detects attacks and application failures, alerts teams via WhatsApp and email, and lets operators safely respond in real time. It correlates aggregate behavior and infrastructure pressure without blaming a browser, country, or single IP.</p></section><section className="grid">{cards.map(([a,b])=><article className="card" key={a}><div className="muted">{a}</div><div className="metric">{b}</div></article>)}</section></main>}
