import{NextRequest,NextResponse}from'next/server';import{randomUUID}from'node:crypto';import{createSession,isSecureRequest,sessionCookie}from'@/lib/session';import{hashPassword}from'@/lib/password';import{store}from'@/lib/store';import type{GuardianUser}from'@/lib/model';
export async function POST(req:NextRequest){const {username,password,displayName}=await req.json() as Record<string,string>;if(!/^[a-zA-Z0-9._-]{3,40}$/.test(username??'')||!displayName?.trim()||!password||password.length<12)return NextResponse.json({error:'Use a valid username and a password of at least 12 characters'},{status:400});if(store.userByName(username))return NextResponse.json({error:'Username already exists'},{status:409});const now=new Date().toISOString(),id=randomUUID();const user:GuardianUser={id,username,displayName:displayName.trim(),role:'admin',status:'active',createdAt:now,updatedAt:now};store.putUser(user,hashPassword(password));const secret=process.env.GUARDIAN_SESSION_SECRET;if(!secret)return NextResponse.json({error:'Sessions are not configured'},{status:503});const token=await createSession({id,username,role:'admin',expiresAt:Date.now()+28800000},secret),res=NextResponse.json({id,username},{status:201});res.cookies.set(sessionCookie,token,{httpOnly:true,sameSite:'strict',secure:isSecureRequest(req),path:'/',maxAge:28800});return res}
