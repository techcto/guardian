import{NextRequest,NextResponse}from'next/server';import{stripe}from'@/lib/stripe';import{store}from'@/lib/store';
export async function POST(req:NextRequest){const signature=req.headers.get('stripe-signature'),secret=process.env.STRIPE_WEBHOOK_SECRET;if(!signature||!secret)return NextResponse.json({error:'webhook not configured'},{status:503});let event;try{event=stripe().webhooks.constructEvent(await req.text(),signature,secret)}catch{return NextResponse.json({error:'invalid signature'},{status:400})}if(event.type==='checkout.session.completed'){const s=event.data.object,userId=s.metadata?.userId,productId=s.metadata?.productId;if(userId&&productId&&typeof s.customer==='string'&&typeof s.subscription==='string')store.putSubscription({id:s.subscription,userId,productId,stripeCustomerId:s.customer,stripeSubscriptionId:s.subscription,status:'active',updatedAt:new Date().toISOString()})}return NextResponse.json({received:true})}
