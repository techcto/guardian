import{NextRequest,NextResponse}from'next/server';import{operator}from'@/lib/operator';import{store}from'@/lib/store';import{stripe}from'@/lib/stripe';
export async function POST(req:NextRequest){const session=await operator(req);if(!session)return NextResponse.json({error:'unauthorized'},{status:401});const {productId}=await req.json() as {productId?:string},product=(await store.products()).find(x=>x.id===productId&&x.active);if(!product?.stripePriceId)return NextResponse.json({error:'This plan is not configured for checkout'},{status:503});const origin=new URL(req.url).origin,checkout=await stripe().checkout.sessions.create({mode:'subscription',line_items:[{price:product.stripePriceId,quantity:1}],client_reference_id:session.id,metadata:{userId:session.id,productId:product.id},success_url:`${origin}/billing?checkout=success`,cancel_url:`${origin}/billing?checkout=canceled`});return NextResponse.json({url:checkout.url})}
