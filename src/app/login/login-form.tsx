'use client';import{FormEvent,useState}from'react';
export default function LoginForm(){const [mode,setMode]=useState<'login'|'signup'>('login'),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const data=new FormData(e.currentTarget),res=await fetch(`/api/auth/${mode}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({displayName:data.get('displayName'),username:data.get('username'),password:data.get('password')})});if(!res.ok){setBusy(false);setError((await res.json()).error??'Unable to continue');return}window.location.href='/'}return <><div className="auth-tabs"><button className={mode==='login'?'active':''} onClick={()=>setMode('login')}>Sign in</button><button className={mode==='signup'?'active':''} onClick={()=>setMode('signup')}>Create account</button></div><form className="login-form" onSubmit={submit}>{mode==='signup'&&<label>Display name<input name="displayName" autoComplete="name" required autoFocus/></label>}<label>Username<input name="username" autoComplete="username" required autoFocus={mode==='login'}/></label><label>Password<input name="password" type="password" minLength={mode==='signup'?12:1} autoComplete={mode==='login'?'current-password':'new-password'} required/></label>{mode==='signup'&&<span className="muted small">Use at least 12 characters. Payment details are collected later by Stripe.</span>}{error&&<p className="form-error" role="alert">{error}</p>}<button className="button primary" disabled={busy}>{busy?'Please wait…':mode==='login'?'Sign in':'Create workspace'}</button></form></>}
