# Contributing

Run `make check` before submitting changes. Add tests for every detector and parser change. Use only synthetic data, reserved example domains/IP ranges, opaque identifiers, and generic addresses. Never commit credentials, customer data, or production logs.
